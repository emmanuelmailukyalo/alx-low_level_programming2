#include "main.h"

/**
 * main - check the code.
 *
 * Return: Always 0.
 */
int print_alphabet_x10(void)

{
	int n;
	char i;

	for (n = 0, n <= 10, n++)
	{
		for (i = 'a', i <= 'z', i++)
			_putchar(i);
		_putchar('\n')
	}
	return (0);
}
